spring:
  datasource:
    username: postgres
    password: pas
    url: jdbc:postgresql://localhost:5433/swapMe-db
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
